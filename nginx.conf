server {
    listen 4089;
    server_name atajan.raxa2.store localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Proxy /api to defense proxy (port 10010)
    # Uses Docker service name "proxy" when running via docker-compose,
    # or host.docker.internal for standalone Docker builds.
    location /api/ {
        proxy_pass http://proxy:10010;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_connect_timeout 5s;
        proxy_read_timeout 30s;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
    gzip_min_length 256;
}

# Bastion Web App

This package defines the target workspace for the production web app layer in the Bastion Twin monorepo architecture.

Current repo migration keeps the active Vite app at the repository root for continuity, while this folder carries package metadata and dependency contracts for the workspace split.

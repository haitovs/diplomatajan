// Preload script - runs in renderer context with limited Node.js access
// Used for secure communication between main and renderer processes

window.addEventListener('DOMContentLoaded', () => {
  console.log('Brute-Force Security Simulator loaded');
});
